@page "/calendar"
@*using System;
using System.Collections.Generic;
using System.Linq;
*@
@code{
    public class StudyTime {
        public int Id {get; set;} 
        public string Title {get; set;} 
        public DateTime StartDate {get; set;} 
        public DateTime EndDate {get; set;} 
        public string Description {get; set;} 
    }
    public class StudyCalendarEdit
    {
        private List<StudyTime> _times = new();

        public List<StudyTime> GetAllTimes() => _times;

        public StudyTime? GetTime(int Id) => _times.FirstOrDefault(e => e.Id == Id);

        public void AddTime (StudyTime newTime)
        {
            newTime.Id = _times.Count > 0 ? _times.Max(e => e.Id) + 1 : 1;
            _times.Add(newTime);
        }
    public void UpdateTime (StudyTime updatedTime)
    {
        var existing = GetTime(updatedTime.Id);
        if (existing != null)
        {
            existing.Title = updatedTime.Title;
            existing.StartDate = updatedTime.StartDate;
            existing.EndDate = updatedTime.EndDate;
            existing.Description = updatedTime.Description; 
        }
    }
    public void DeleteEvent (int id)
    {
        var ev = GetTime(id);
        if (ev != null)
        {
            _times.Remove(ev);
        }
    }
}
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Study Sprout Calendar</title>
  <link rel="stylesheet" href="calendar.css" />
</head>
<body>
  <div class="calendar-wrapper">
    <header>
      <div class="dropdown">
        <select>
          <option>Pick an option</option>
        </select>
      </div>
      <h1>March <span>2021</span></h1>
      <div class="logo">🌱 Study Sprout</div>
    </header>

    <div class="calendar">
      <div class="day-labels">
        <div>Sun</div><div>Mon</div><div>Tue</div><div>Wed</div><div>Thu</div><div>Fri</div><div>Sat</div>
      </div>
      <div class="days">
        
        <div class="day blank"></div><div class="day blank"></div><div class="day">1<br><span class="event orange">Groomers appt.</span></div>
        <div class="day">2</div><div class="day">3<br><span class="event red">Meeting w/ Chris</span></div>
        <div class="day">4<br><span class="event blue">Lunch w/ Mom</span></div><div class="day">5</div>
        
      </div>
    </div>
  </div>
</body>
</html>

}

body {
  font-family: 'Segoe UI', sans-serif;
  background-color: #222;
  margin: 0;
  padding: 2rem;
}

.calendar-wrapper {
  background: white;
  border-radius: 30px;
  max-width: 1000px;
  margin: auto;
  padding: 2rem;
  border: 15px solid #9acd8f;
}

header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 1rem;
}

h1 {
  font-size: 2rem;
  color: #3a7c3f;
}

h1 span {
  color: #888;
  font-weight: normal;
}

.logo {
  font-weight: bold;
  font-size: 1.5rem;
  color: #3a7c3f;
}

.dropdown select {
  padding: 0.5rem;
  border-radius: 5px;
}

.calendar {
  display: flex;
  flex-direction: column;
}

.day-labels, .days {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  gap: 10px;
  text-align: center;
}

.day-labels div {
  font-weight: bold;
  color: #555;
}

.day {
  border: 1px solid #ddd;
  min-height: 100px;
  position: relative;
  padding: 5px;
  text-align: left;
  font-size: 0.9rem;
  border-radius: 8px;
  background: #fdfdfd;
}

.blank {
  background: none;
  border: none;
}

.event {
  display: block;
  margin: 4px 0;
  padding: 3px 6px;
  border-radius: 5px;
  font-size: 0.75rem;
  color: #fff;
}

.orange {
  background: #f3d6a4;
  color: #754c00;
}

.red {
  background: #f4c4c4;
  color: #a10000;
}

.blue {
  background: #cce5ff;
  color: #004085;
}

